# OpenArm V2.0 Arms

OpenArm by Enactic (https://openarm.dev).

This package contains the left and right OpenArm V2.0 arm geometry and
SuperDex `.superdex_bot` files.

## License scope

Enactic granted permission to distribute these OpenArm V2.0 package files under
Apache-2.0:

- `left/cad/*.stp` and `right/cad/*.stp`
- `left/render/*.glb` and `right/render/*.glb`
- `left/collision/*.mochi.h5` and `right/collision/*.mochi.h5`
- `left/*.superdex_bot` and `right/*.superdex_bot`

See `LICENSE` for the Apache License 2.0 and `NOTICE` for attribution.
