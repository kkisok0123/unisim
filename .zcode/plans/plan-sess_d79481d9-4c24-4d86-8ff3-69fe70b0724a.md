# Stage 1: Copy the local Superdex assets into `unisim/assets/superdex` and verify the result

Follows `assets/superdex/superdex-assets-integration-plan.md` §"Copy the local assets and verify the result", with your amendment: the copy lives in **`/home/pc829/UniFamily/unisim/assets/superdex/`** (the plan doc's flat `assets/bots/...` layout is superseded — its "Required destination layout" section gets a small edit to match). All tooling is implemented from scratch; no recovery of earlier workstreams. Final state left **uncommitted** for your review.

## Basis (from exploration)

- **Source**: `/home/pc829/UniFamily/project_superdex/assets/` — 248 MiB / 891 files, clean git tree (branch `wuji-actuated-asset`, HEAD `6b0541b`). Layout: `bots/ prefabs/ benchmarks/ cube/ test/`; 37 `.superdex_bot`, 15 `.mochi_prefab`, 2 `.mochi_scene`, 1 `.superdex_controller`. No symlinks/caches; 3 empty `.superdex_root` markers must survive.
- **References to verify**: bot-file-relative (`collision/...`, `render/...`), `//hands/...` against `.superdex_root` markers, `./`-relative nested prefabs, bundle-root-relative scene/ant meshes.
- **Destination**: `assets/superdex/` exists with only the plan doc; incoming dirs collide with nothing and the plan doc stays untouched.
- **Packaging**: `uv_build` packages only `src/unisim`; `assets/` cannot enter `unisim-core` — still verified via `make package`.

## Steps

### 1. New SDK-free inventory module (`src/unisim/backend/superdex/assets.py`)
From scratch, stdlib-only (`hashlib/json/dataclasses/pathlib`), never imported from `unisim/__init__`.
- Frozen-dataclass entries for bots, prefabs, scenes, controllers; dependency edges with source fields.
- Provenance = actual local source: repo path, branch, HEAD `6b0541b…`, clean tree, per-file SHA-256 + deterministic tree digest, local-derivative attribution vs. the source repo's initial import `1d71509`. Historical upstream pin `ed30ce1…` is reference info only.
- Reference resolution validated empirically before locking rules: manifest-relative, `//` marker-relative, `./` nested-prefab, bundle-root-relative. Escapes, missing files, and git-LFS pointers are errors.
- Capability + support status per entry mapped to adapter stages (profile-candidate / recipe-candidate / blocked-with-named-capability / rigid-prefab / scene / control-profile); unsupported entries preserved with precise blockers, never dropped.
- Deterministic JSON serialization.

### 2. New copy + verify script (`scripts/copy_superdex_assets.py`)
Pre-flight conflict check (refuse differing existing paths; never touch unrelated files like the plan doc), real byte copy of all 891 files including hidden markers into `assets/superdex/` (final layout `assets/superdex/bots/…` etc., no `assets/assets/` nesting), then verification: identical relative-path sets and SHA-256 source↔destination, inventory + dependency resolution run on the destination alone, reports written under `docs/`. Nonzero exit on any error; idempotent re-run.

### 3. Tests (`tests/test_superdex_assets.py`)
Synthetic `tmp_path` bundles, SDK-free, in the normal `make check` suite: entry classification and dependency edges for all four formats, every reference-resolution rule, escape/missing/LFS rejection, disposition/blocker assignment, copy conflicts and unrelated-file preservation, hash equality, deterministic reports.

### 4. Run it
Copy produces `assets/superdex/{bots,prefabs,benchmarks,cube,test}/` plus `docs/superdex-assets-inventory.{md,json}` (provenance, counts, per-entry capabilities/status, dependency-resolution and hash results). All references resolve from the destination alone; runtime FR3 loading is stage 2, with `SUPERDEX_ASSETS_PATH=/home/pc829/UniFamily/unisim/assets/superdex`.

### 5. Docs, changelog, checks
- `docs/superdex.md`: section on the repo-local asset tree at `assets/superdex/` and the `SUPERDEX_ASSETS_PATH` value.
- `assets/superdex/superdex-assets-integration-plan.md`: update the "Required destination layout" section to the amended `assets/superdex/` destination.
- `CHANGELOG.md`: Unreleased entry.
- `make check`; `make package` + sdist listing to prove no asset leakage. Working tree left uncommitted; final summary lists all new/changed paths and `git status`.

## Done-when
Every source asset present at the matching path under `assets/superdex/` with identical contents; required dependencies resolve from the destination; every entry has provenance and required capabilities recorded.

## Notes
- Scene/ant bundle-root-relative semantics verified empirically before the resolver rule is finalized.
- ~248 MiB as plain git blobs per your choice; nothing in `.gitignore` blocks it.
- Nothing from earlier workstreams (dangling stash objects, stale `__pycache__`) is read, restored, or deleted.